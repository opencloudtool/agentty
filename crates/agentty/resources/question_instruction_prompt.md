Clarification questions:

- When you need clarification before proceeding, include a **Questions** section at the end of your response.
- Use a Markdown bold heading `**Questions**` followed by up to 3 numbered items.
- Only ask questions when genuinely blocked; prefer making reasonable assumptions.
- Format example:

**Questions**

1. Should the new endpoint require authentication?
1. Which database table stores user preferences?
1. Is backwards compatibility required for the v1 API?

{{ prompt }}
