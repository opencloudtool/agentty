# Demo Recording

The `demo.gif` file is generated from `demo.tape` using [VHS](https://github.com/charmbracelet/vhs).

From this directory, run:

```bash
vhs demo.tape
```

This command re-renders `demo.gif` from the scripted terminal recording in `demo.tape`.
