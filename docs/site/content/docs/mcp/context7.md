+++
title = "Context7"
description = "Add Context7 to Codex via MCP."
aliases = ["/docs/mcp/codex/"]
weight = 1
+++

Use `Context7` via MCP so Codex can access up-to-date library documentation.

<!-- more -->

## Codex

For the simplest setup from the official Codex MCP docs, run:

```bash
codex mcp add context7 -- npx -y @upstash/context7-mcp
```

If you need to verify the server was added:

```bash
codex mcp list
```

Reference: [OpenAI Codex MCP guide](https://developers.openai.com/codex/mcp/).
